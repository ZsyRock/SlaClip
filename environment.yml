name: opacus
channels:
  - conda-forge
dependencies:
  - python=3.10
  - numpy
  - tqdm
  - opt_einsum
  - pytest
  - datasets
  - transformers
  - pip
  - pip:
      - --index-url https://download.pytorch.org/whl/cu128
      - torch==2.10.0
      - torchvision==0.25.0
      - torchaudio==2.10.0
